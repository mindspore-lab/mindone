model_version: v1.2
ckpt_path: "models/opensora_stdit_v3.ckpt"
t5_model_dir: "models/t5-v1_1-xxl/"
vae_checkpoint: "models/opensora_vae_v2.ckpt"

resolution: 240p
aspect_ratio: 9:16
num_frames: 4s
fps: 24
enable_flash_attention: True
model_max_length: 300
dtype: "bf16"
batch_size: 1

# sampling
sampling_steps: 30
guidance_scale: 7.0
guidance_channels: 3
seed: 42
ddim_sampling: False

loop: 1
condition_frame_length: 5

captions:
  - "In an ornate, historical hall, a massive tidal wave peaks and begins to crash. Two surfers, seizing the moment, skillfully navigate the face of the wave."

mask_strategy:
  # 6 numbers: loop index, reference id, reference starting frame, position in the generated video,
  # number of conditioning frames, editing strength. For more, see docs/quick_start.md#imagevideo-to-video
  - "0, 0, 0, 0, 1, 0"

reference_path:
  - "assets/wave.png"
