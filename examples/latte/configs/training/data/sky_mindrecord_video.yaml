data_config:
  train_data_type: "mindrecord"
  data_folder: "path/to/sky_train_cache"
  sample_stride: 3
  use_safer_augment: False
  image_video_joint: False
  use_image_num:
  num_parallel_workers: 8
  max_rowsize: 64
  shuffle: True
